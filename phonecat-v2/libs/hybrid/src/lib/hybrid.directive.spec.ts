import { HybridDirective } from './hybrid.directive';

describe('HybridDirective', () => {
  it('should create an instance', () => {
    const directive = new HybridDirective();
    expect(directive).toBeTruthy();
  });
});
